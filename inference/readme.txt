Inference PETRA

``cd lm``

use ``bash inference_petra.sh`` for petra model inference, it will start a server at localhost:5001, you can pass parameters and get predicted results.

use ``bash inference_petra_large.sh`` for petra-large model inference. 